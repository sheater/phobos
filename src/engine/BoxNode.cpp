// #include "BoxNode.h"
// #include "Geometry.h"

// BoxNode::BoxNode(Scene* scene, const glm::vec3 &size)
//     : SceneNode(scene), m_size(size), material(nullptr)
// {
//   // Geometry *geometry = Geometry::createBox(m_size);
//   // m_vertexBuffer = m_renderer->createVertexBuffer(geometry);
//   // delete geometry;
// }

// void BoxNode::render()
// {
//   // const glm::mat4 finalModelMatrix = transform * dynamicMatrix * staticMatrix;
//   // m_vertexBuffer->render(finalModelMatrix, adjustmentsMatrix, material);
// }
